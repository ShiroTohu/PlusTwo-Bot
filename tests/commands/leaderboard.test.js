const leaderboard = require('../../source/commands/scores/leaderboard');
const { beforeEach } = require('../beforeEach')

test('testEmbedField', () => {
    const fields = [];
    expect(true).toBe(true);
});

